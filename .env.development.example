# Configurações de desenvolvimento
# Copie este arquivo para .env.development e ajuste conforme necessário

# URL do backend da API para proxy do Vite
# Opções:
# - API em Docker (mesma rede): http://projeto-base-nginx:80 (recomendado quando ambos estão em Docker)
# - API no host local: http://host.docker.internal:8080
# - API externa: http://api.example.com
VITE_API_PROXY_TARGET=http://projeto-base-nginx:80

# Nome da aplicação (exibido no título e páginas)
VITE_APP_NAME="Projeto Base"

# Base utilizada pelo axios/httpClient. Mantendo "/api" aproveitamos o proxy local
# e reproduzimos a rota relativa esperada em produção atrás de um reverse proxy.
VITE_API_BASE_URL=/api
